# 岱境235 双模式授权配置说明

**文档版本**：V1.0
**生效日期**：2026-09-01
**裁决依据**：姜总裁决二（2026-09-01）
**适用范围**：V2.5 沙箱及后续所有派生版本（V2.2-S 内部历史归档版不含此能力，维持 Fail-Closed 单模式）
**文档性质**：对外可交付（第四节法务条款须经法务复核后方可随合同附件发出）

---

## 一、背景与问题定义

### 1.1 冲突根源

2026-09-01 V2.5 储备加固（措施6）将授权失败语义由 **Fail-Open（降级只读）** 翻转为 **Fail-Closed（拒绝启动）**，从安全工程角度彻底封死了"无授权亦可运行"的口子。

但该系统定义中长期锁定的**合同核心免责条款**为：

> 授权到期后，系统绝不断网崩溃，只自动降级为只读模式。

该条款语义为 **Fail-Open**。由此产生一项实质性法律风险：

| 维度 | 合同条款承诺 | V2.5 引擎实际行为 | 后果 |
|---|---|---|---|
| 授权到期 | 降级只读，业务可查 | 拒绝启动，进程终止 | **我方违约** |

### 1.2 解决方案：双模式可配置

将授权失败语义从"编译期硬编码"改为"运行期可配置"，由环境变量 `LICENSE_FAIL_MODE` 决定：

- **出厂默认 `closed`** —— 与 V2.5 加固后的安全语义一致，保护我方知识产权；
- **可选 `open`** —— 与既有合同免责条款一致，仅在甲方已签署补充条款后启用。

工程成本近乎为零，商业弹性最大化。

---

## 二、模式定义与行为对照

### 2.1 触发条件（两模式完全一致）

以下任一情形判定为"授权校验失败"：

1. 未找到 `license.key` 授权文件
2. 授权文件缺少 RSA-2048 签名
3. RSA 签名验证失败（授权被篡改）
4. 商业授权硬件绑定指纹与当前机器指纹不匹配
5. 检测到时间回拨攻击（当前时间早于签发时间）
6. 授权已过期

### 2.2 行为对照表

| 项目 | `closed`（默认） | `open`（受限） |
|---|---|---|
| **语义名称** | Fail-Closed | Fail-Open |
| **进程行为** | `log.Fatal` 立即终止 | 置位降级标志后继续运行 |
| **进程退出码** | `1` | 不退出 |
| **Raft 选举 / 心跳** | 不执行（进程已终止） | ✅ 正常 |
| **日志复制** | 不执行 | ✅ 正常 |
| **只读查询** | 不执行 | ✅ 正常 |
| **写入请求** | 不执行 | ❌ 拒绝（`PermissionDenied`） |
| **成员变更** `/cluster/add` `/cluster/remove` | 不执行 | ❌ 拒绝（`403 Forbidden`） |
| **健康检查** `/health/live` | 不执行 | ✅ 正常 |
| **适用合同形态** | 授权停机条款 | 降级只读免责条款 |
| **可否出厂默认** | ✅ **是** | ❌ 否 |

### 2.3 关键设计：严格白名单回落

环境变量解析采用**大小写敏感精确匹配**：

| 取值 | 解析结果 | 说明 |
|---|---|---|
| `open` | `open` | 唯一可触发降级的取值 |
| `closed` | `closed` | 显式关闭 |
| 未设置（空） | `closed` | 回落安全侧 |
| `OPEN` / `Open` | `closed` | 大小写变体一律回落 |
| `opne` / `true` / `1` / 任意非法值 | `closed` | 拼错即回落，杜绝误降级 |

**设计原则：回落方向恒为安全侧。宁可拒绝启动，不可无授权运行。**

---

## 三、配置方法

### 3.1 环境变量规范

```
键名：LICENSE_FAIL_MODE
取值：closed | open
默认：closed
大小写：敏感（仅小写有效）
```

### 3.2 三种部署形态

**形态一：Docker Compose（推荐）**

已内置于 `docker-compose.yml` 全部 5 个节点：

```yaml
environment:
  NODE_ID: node-1
  GRPC_PORT: "9500"
  HTTP_PORT: "9000"
  # 双模式授权防线：closed=授权失败拒绝启动（出厂默认）
  # 严禁改为 open，除非甲方已签署并生效《授权到期降级只读补充条款》
  LICENSE_FAIL_MODE: "closed"
```

**形态二：systemd 守护进程**

已内置于 `daijin235_cluster.service`：

```ini
[Service]
Restart=always
RestartSec=10
Environment=LICENSE_FAIL_MODE=closed
```

**形态三：裸机 / 容器直启**

使用权威模板 `daijin235.env.default`：

```bash
export $(grep -v '^#' daijin235.env.default | xargs)
./gateway -id node-1 -port 9500 -http 9000
```

### 3.3 镜像层固化（Dockerfile）

已内置 `ENV LICENSE_FAIL_MODE=closed`，**镜像构建期即固化默认值**，运行期即使不传环境变量也恒为 `closed`。

---

## 四、⚠ 开启 `open` 模式的前置条件与法务红线

> **本节省略或绕过，将直接构成合同违约。** 任何人员不得在未完成以下全部条件前将 `LICENSE_FAIL_MODE` 置为 `open`。

### 4.1 三项前置条件（缺一不可）

| 序号 | 条件 | 验证方式 |
|---|---|---|
| 1 | 甲方已签署《授权到期降级只读补充条款》且条款已生效 | 查验双方法务盖章件扫描件 |
| 2 | 主合同免责条款已同步修订为"降级只读"表述 | 与补充条款交叉比对 |
| 3 | 经岱境235 授权管理人（姜国新）书面确认 | 书面指令留痕 |

**未与甲方签订补充条款前，严禁开启 `open` 模式。**

### 4.2 法务红线（不可协商）

1. **红线一**：`open` 模式不得以任何形式作为出厂默认配置。默认交付模板、镜像、systemd 单元中的值恒为 `closed`。
2. **红线二**：`open` 模式不得通过口头指令开启，必须有书面授权记录并归档。
3. **红线三**：`open` 模式开启后，须在交付文档中向甲方明示"写入功能已停用，仅保留只读查询与集群一致性维护"。
4. **红线四**：授权过期引发的降级运行，不得被解释为"系统已获得合法授权"。`open` 模式仅为业务连续性缓冲，不构成授权豁免。
5. **红线五**：客户现场若发现 `LICENSE_FAIL_MODE=open` 且无对应补充条款存档，视为**交付事故**，须立即整改并上报。

### 4.3 建议商务话术

> "我们的引擎支持两种授权终止策略。标准交付采用 Fail-Closed——授权失效即停机，这是我们保护客户数据完整性的默认设计。若贵方业务连续性要求更高，我们可以签署一份《授权到期降级只读补充条款》，届时系统会切换为 Fail-Open：授权到期后不中断服务，自动降级为只读模式，历史数据可查、集群一致性照常维护，仅停用写入。这个开关是在交付模板里配置的，随时可查、可审计。"

---

## 五、运行期自证与审计

### 5.1 自证端点

新增只读端点 `GET /license/status`，供甲方独立核验当前运行模式：

```json
{
  "fail_mode": "open",
  "fail_mode_env": "LICENSE_FAIL_MODE",
  "fail_mode_raw": "open",
  "default_mode": "closed",
  "degraded": true,
  "degraded_reason": "授权校验失败(Fail-Open)：...",
  "semantics": {
    "closed": "授权失败即拒绝启动(Fail-Closed)",
    "open": "授权失败降级为只读运行(Fail-Open)"
  }
}
```

### 5.2 启动日志留痕

```
[授权] 失败处理模式: closed（环境变量 LICENSE_FAIL_MODE 未设置，回落出厂默认 closed）
```

```
[授权] 失败处理模式: open（环境变量 LICENSE_FAIL_MODE="open"）
[授权] ⚠ 法务提示：open 模式须以《授权到期降级只读补充条款》已签署生效为前提。
[授权] 已置位全局降级只读标志：写入请求与成员变更将被拒绝，选举与心跳保持正常。
```

### 5.3 降级运行警示横幅

`open` 模式下进程持续输出警示横幅，closed 模式永不进入该分支：

```
!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
  ⚠  降级只读模式运行中（Fail-Open）
  原因: 授权校验失败(Fail-Open)：...
  约束: 写入请求与集群成员变更已被拒绝
         Raft 选举 / 心跳 / 只读查询保持正常
  法务: 仅在《授权到期降级只读补充条款》生效下允许
!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
```

---

## 六、物理验证证据链

### 6.1 源码改动与备份

| 文件 | 改动前 MD5 | 改动前字节 | 改动后 MD5 | 改动后字节 |
|---|---|---|---|---|
| `main.go` | `77ed18cd27265e6b7e7170d1f51ca2c7` | 10,102 | `3c8c043784cc0d3c3515279656c705de` | 13,541 |
| `license_guard.go` | `345d13b21c09709bd9c48944b21f30d3` | 8,652 | `683ba83dc8458d2b565d77508bdbe328` | 11,170 |

全部改动文件已生成 `.bak_v25_20260901_205914` 备份，支持一键回滚。

### 6.2 编译验证

```
go version go1.25.0 windows/amd64
go build -mod=vendor -o _v25_failmode_build.exe .
退出码: 0
产物: 19,389,952 字节 | MD5=622a0fd2d62e4c13d55f85d7fe99df9f
gofmt: 无差异
```

### 6.3 运行时行为实测

**测试组 A — closed 模式（应拒绝启动，退出码 1）**

| 用例 | `LICENSE_FAIL_MODE` | 解析模式 | 退出码 | 结论 |
|---|---|---|---|---|
| A1 | 未设置 | `closed` | 1 | ✅ 拒绝启动 |
| A2 | `closed` | `closed` | 1 | ✅ 拒绝启动 |

**测试组 B — 回落矩阵（全部应回落 closed 并拒绝启动）**

| 用例 | 取值 | 解析模式 | 退出码 | 结论 |
|---|---|---|---|---|
| B1 | `""` | `closed` | 1 | ✅ |
| B2 | `OPEN` | `closed` | 1 | ✅ |
| B3 | `Open` | `closed` | 1 | ✅ |
| B4 | `opne` | `closed` | 1 | ✅ |
| B5 | `true` | `closed` | 1 | ✅ |
| B6 | `1` | `closed` | 1 | ✅ |
| B7 | `closed` | `closed` | 1 | ✅ |
| B8 | `"  closed  "` | `closed` | 1 | ✅ |

**测试组 C — open 模式（应降级运行，进程存活）**

| 用例 | 取值 | 解析模式 | 进程状态 | 结论 |
|---|---|---|---|---|
| C1 | `open` | `open` | 存活（timeout 触发 124） | ✅ 降级运行 |
| C2 | `"  open  "` | `open` | 存活 | ✅ 容忍空白字符 |

**测试组 D — 端点自证（open 模式下）**

```
GET /license/status →
  fail_mode = "open"
  default_mode = "closed"
  degraded = true
```
✅ 与预期一致

### 6.4 红线零触碰复检

| 资产 | MD5 | 状态 |
|---|---|---|
| 权威主线 `raft.go` | `85145663d0d6ab77ee70adf9961dc124`（24,649 字节） | ✅ 未变 |
| V2.2-S 定版二进制 | `a6a25a3c9aba3ee121147e9dda47357b`（12,255,416 字节） | ✅ 未变 |
| 商业部署包 | `b1187916a40265e5c984ccc04c03158b` | ✅ 未变 |
| 终极证据包 | `cdcd5715e4c37f84c2cef579c1ed78be` | ✅ 未变 |
| D 盘私钥 `daijin235_rsa_private.pem` | 沙箱内命中数 **0** | ✅ 零暴露 |

本次改动**全部位于 V2.5 沙箱** `D:\235备份文件\V2.4_Performance_Sandbox\`，V2.2-S 权威主线零字节改动。

---

## 七、故障处置

| 现象 | 诊断 | 处置 |
|---|---|---|
| 启动即退出，日志显示 Fail-Closed | 授权缺失/过期/指纹不匹配 | 核验 `license.key`，重新签发 |
| 已设 `open` 但仍拒绝启动 | 取值大小写错误或拼错 | 严格使用小写 `open` |
| `open` 生效但写入被拒 | 预期行为 | 如需写入，须恢复有效授权 |
| 客户质疑当前模式 | 需自证 | 引导访问 `GET /license/status` |
| 发现现场为 `open` 无补充条款 | 交付事故 | 立即改回 `closed`，上报并整改 |

---

## 八、回滚方法

```bash
cd D:\235备份文件\V2.4_Performance_Sandbox
cp main.go.bak_v25_20260901_205914 main.go
cp license_guard.go.bak_v25_20260901_205914 license_guard.go
cp docker-compose.yml.bak_v25_20260901_205914 docker-compose.yml
cp Dockerfile.bak_v25_20260901_205914 Dockerfile
cp daijin235_cluster.service.bak_v25_20260901_205914 daijin235_cluster.service
rm -f daijin235.env.default
go build -mod=vendor -o gateway .
```

回滚后引擎恢复为 V2.5 单模式 Fail-Closed（无环境变量开关）。

---

**文档结束**
**编制**：岱境235 首席技术合伙人 / 资深 SRE 架构师
**核验方式**：实机终端原始输出贴回，MD5 / 字节数为精确证据链
**编制时间**：2026-09-01 21:15 (CST, UTC+8)
