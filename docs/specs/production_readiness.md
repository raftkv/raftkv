# 生产级就绪差距清单

> 盘点时间：2026-09-11
> 盘点标准："这个系统能不能进生产"
> 当前版本：batch13（v2.4-post-batch13），commit 6e890e8
> 归零复测：3min c=128 TPS=8240.4 / 成功率=100% / P99=50ms / 5/5 存活 / 0 次 leader 切换

---

## 1. 可观测性

### 1.1 Metrics 端点

| 指标 | 现状 | 差距 | 建议批次 |
|------|------|------|----------|
| TPS 导出 | /latency/metrics 有 Prometheus 格式延迟直方图 | 无独立 TPS counter 导出 | batch15 |
| 延迟分位（P50/P95/P99） | /latency/stats JSON + /latency/metrics Prometheus | 已就绪 | — |
| 心跳统计 | /raft/stats 有 consecutiveTimeouts/consecutiveSuccess | 未导出 Prometheus 格式 | batch15 |
| 选举统计 | /raft/stats 有 term/state/leaderID | 未导出 Prometheus 格式 | batch15 |
| compaction 计数 | 日志中有"异步快照完成"计数 | 无端点导出 compaction 次数/logStartIndex | batch15 |
| 统一 Prometheus 端点 | 无 /metrics 统一端点 | 各指标分散在多个端点，需统一为 /metrics | batch15 |

### 1.2 结构化日志

| 指标 | 现状 | 差距 | 建议批次 |
|------|------|------|----------|
| 日志格式 | 标准库 log.Printf（文本格式） | 无 JSON 结构化日志，无法接入 ELK/Loki | batch16 |
| 日志级别 | 无级别区分 | 无 DEBUG/INFO/WARN/ERROR 分级 | batch16 |
| 请求追踪 | 无 traceID/requestID | 无法追踪单请求全链路 | batch16 |
| 慢查询追踪 | 无 | 无慢查询检测与告警 | batch16 |

---

## 2. 运维面

### 2.1 优雅启停

| 指标 | 现状 | 差距 | 建议批次 |
|------|------|------|----------|
| 优雅停止 | 有 SIGINT/SIGTERM 处理 + httpSrv.Shutdown(5s) + node.Shutdown() | 已就绪 | — |
| leader 转移 | 停止时 node.Shutdown() 触发 stepDown | 无主动 leader 转移（等待选举超时），停机期间写入不可用 ~5s | batch15 |
| 启动恢复 | WAL 回放 + 快照恢复 | 已就绪 | — |

### 2.2 节点替换流程

| 指标 | 现状 | 差距 | 建议批次 |
|------|------|------|----------|
| 添加节点 | /cluster/add 端点 | 需手动操作，无自动化流程 | batch16 |
| 移除节点 | /cluster/remove 端点 | 需手动操作，无数据迁移 | batch16 |
| 节点替换 | 无 | 无替换流程文档，无滚动替换支持 | batch16 |

### 2.3 配置热更新

| 指标 | 现状 | 差距 | 建议批次 |
|------|------|------|----------|
| 参数热更新 | 无 | 所有参数启动时固定（electionTimeout/heartbeatInterval/snapshotThreshold 等） | batch16 |
| 配置中心 | 无 | 无集中配置管理 | batch17+ |

### 2.4 版本升级/回滚

| 指标 | 现状 | 差距 | 建议批次 |
|------|------|------|----------|
| 滚动升级 | 无 | 无滚动升级方案，需全集群停机 | batch16 |
| 回滚 | git tag + git bundle | 需手动操作，无自动化回滚 | batch16 |
| 版本兼容性 | 无版本协商 | 升级期间混合版本可能不兼容 | batch17+ |

---

## 3. 数据面

### 3.1 磁盘管理

| 指标 | 现状 | 差距 | 建议批次 |
|------|------|------|----------|
| WAL 磁盘管理 | WAL 有 segment 轮转 + 快照后截断 | 已就绪 | — |
| 磁盘满行为 | 无处理 | 磁盘满时写入失败但无降级策略 | batch15 |
| 磁盘监控 | 无 | 无磁盘使用率监控/告警 | batch15 |

### 3.2 备份恢复

| 指标 | 现状 | 差距 | 建议批次 |
|------|------|------|----------|
| WAL 备份 | WAL 文件可手动复制 | 无自动化备份 | batch16 |
| 快照备份 | snapshot.gz 可手动复制 | 无自动化备份 | batch16 |
| 恢复演练 | 无 | 无备份恢复演练流程 | batch16 |

### 3.3 数据校验

| 指标 | 现状 | 差距 | 建议批次 |
|------|------|------|----------|
| startIdx 对照 | /raft/stats 有 logStartIndex | 已就绪（batch13 [SYNC] 核对通过） | — |
| 日志完整性 | WAL 回放时有完整性检查 | 已就绪 | — |
| SM3 链校验 | 有 SM3Hash 字段 | 已就绪 | — |

---

## 4. 极限与降级

### 4.1 容错实测

| 指标 | 现状 | 差距 | 建议批次 |
|------|------|------|----------|
| 磁盘慢 | 无实测 | 未测试磁盘 I/O 延迟升高时的行为 | batch15 |
| 网络分区 | 无实测 | 未测试网络分区时的 leader 选举/脑裂防护 | batch15 |
| 时钟漂移 | 无实测 | electionTimeout 依赖本地时钟，未测试时钟漂移影响 | batch17+ |
| 故障切换 | batch13 测试：kill follower <5s 恢复 | 已就绪 | — |

### 4.2 过载保护

| 指标 | 现状 | 差距 | 建议批次 |
|------|------|------|----------|
| c=256 行为 | P99=100ms，成功率=100%，5/5 存活 | 无降级策略，高并发时延迟升高但无拒绝/限流 | batch15 |
| 限流 | 无 | 无请求限流/背压机制 | batch15 |
| 背压 | replicateCh 缓冲 256 | channel 满时阻塞，无显式背压策略 | batch15 |

---

## 5. 安全

### 5.1 节点间通信

| 指标 | 现状 | 差距 | 建议批次 |
|------|------|------|----------|
| gRPC 认证 | 无 TLS | 节点间通信明文，无 mTLS | batch15 |
| 通信加密 | 无 | 无传输层加密 | batch15 |
| 节点身份 | 节点 ID 硬编码 | 无证书/密钥认证 | batch15 |

### 5.2 客户端鉴权

| 指标 | 现状 | 差距 | 建议批次 |
|------|------|------|----------|
| 写入鉴权 | 无 | /raft/propose 无鉴权 | batch15 |
| 读取鉴权 | 无 | /raft/get 无鉴权 | batch15 |
| 幂等 token | 有 idem_token | 已就绪（防重复提交） | — |
| WAL 加密 | SM4-CTR 加密 | 已就绪 | — |

---

## 总结

### 生产级就绪度评估

| 维度 | 就绪度 | 主要差距 |
|------|--------|----------|
| 可观测性 | 40% | 缺统一 Prometheus 端点、结构化日志、链路追踪 |
| 运维面 | 30% | 缺配置热更新、滚动升级、节点替换自动化 |
| 数据面 | 60% | 缺磁盘满处理、备份恢复演练 |
| 极限与降级 | 40% | 缺网络分区/磁盘慢实测、限流机制 |
| 安全 | 20% | 缺 mTLS、客户端鉴权 |

### 建议批次归属

| 批次 | 主要内容 |
|------|----------|
| batch15 | 统一 Prometheus /metrics 端点 + mTLS + 客户端鉴权 + 限流 + 磁盘满处理 + 网络分区实测 |
| batch16 | 结构化日志 + 链路追踪 + 滚动升级 + 节点替换 + 备份恢复演练 |
| batch17+ | 配置中心 + 时钟漂移 + 版本兼容性 |