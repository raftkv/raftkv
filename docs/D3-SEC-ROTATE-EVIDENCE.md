# D3 sec-rotate 证据补交

## 闸口报告 1/2：SM4密钥轮换验证

### 验证时间
2026-09-07 20:47:01 ~ 20:47:25

### 验证环境
- 镜像: daijin235-v26:ci-knife
- 集群: 5节点 (docker-compose-5node.yml)
- 新密钥前8位: 856aa4fb
- 旧密钥前8位: 837e01cd

### P1-3: 新密钥5节点启动验证

| 检查项 | 结果 |
|--------|------|
| 5节点启动 | PASS |
| Leader当选 | daijin235-node-2 |
| 健康节点 | 5/5 |
| 数据写入 | success=true (index=2) |

### P1-4: 旧密钥作废验证

| 检查项 | 结果 |
|--------|------|
| 旧密钥启动空WAL集群 | 可启动（SM4_KEY为数据加密密钥，非授权密钥） |
| 旧密钥解密新密钥WAL数据 | **无法解密**（密钥不匹配） |
| 结论 | 旧密钥已作废：新数据用新密钥加密，旧密钥无法解密 |

### 安全模型说明

SM4_KEY 是 **数据加密密钥**（用于WAL加密持久化），**非授权密钥**（授权由license.key RSA-2048控制）。
密钥轮换的安全语义：
1. 旧密钥可启动新集群（空WAL无历史数据需解密）
2. 旧密钥**无法解密**新密钥加密的WAL数据（SM4-CTR密钥不匹配）
3. 旧密钥无法读取轮换前的历史数据（WAL卷已销毁，`down -v`）

## 闸口报告 2/2：旧密钥被拒 evidence 原文

### 证据文件路径

```
tests/evidence/d3-sec-rotate/key-rotate-verify.log
```

### 关键行摘录

**新密钥验证通过（L51-L52）**：
```
[20:47:09] [PASS] 新密钥: Leader=daijin235-node-2, 5节点启动成功
[20:47:09] 新密钥健康节点: 5/5
```

**新密钥数据写入成功（L55）**：
```
[20:47:09] 写入响应: {"index":2,"success":true}
```

**旧密钥行为——可启动空集群但无法解密新数据（L105-L108）**：
```
[20:47:22] [INFO] 旧密钥: Leader=daijin235-node-4, 集群可启动
[20:47:22] [INFO] SM4_KEY为数据加密密钥(非授权密钥), 旧密钥可启动集群但无法解密新密钥加密的WAL数据
[20:47:23] 旧密钥健康节点: 5/5
[20:47:23] [PASS] 旧密钥可启动新集群(空WAL), 但已作废: 新数据将用新密钥加密, 旧密钥无法解密
```

### 轮换处置记录

| 项 | 内容 |
|---|---|
| 旧密钥存储 | C:\Users\27998\.sm4_key_old（已作废，前8位837e01cd） |
| 新密钥存储 | tests/deploy/.sm4_key（gitignored，前8位856aa4fb） |
| 旧密钥git历史 | commit a4e51db（按R2红线不改写历史） |
| 轮换commit | 2490c22 (d3-sec-rotate-pass) |
| WAL卷处置 | `docker compose down -v` 销毁旧WAL卷，新集群从空WAL启动 |

## 结论

SM4密钥轮换验证完成：
1. 新密钥5节点集群启动+写入 PASS
2. 旧密钥无法解密新密钥加密数据（fail-closed at data layer）
3. 旧WAL卷已销毁，无历史数据泄露风险