# RaftKV Quickstart Deployment — 5-Node Cluster
#
# Simplified docker-compose for quick evaluation. Builds from source.
# Uses LICENSE_FAIL_MODE=open (degraded read-only mode) for demo purposes.
#
# Usage:
#   docker compose -f examples/docker-compose-quickstart.yml up -d
#   go run examples/kv_client/main.go http://localhost:9001
#
# Note: In degraded mode, writes are rejected. To enable writes,
# generate a license key using cmd/license-tool and mount it into
# the containers, then set LICENSE_FAIL_MODE=closed.

services:
  node-1:
    build: ..
    container_name: raft-node-1
    ports:
      - "9001:9000"
      - "9501:9500"
    environment:
      - SM4_KEY=raftkv_sm4test01
      - NODE_ID=node-1
      - GRPC_PORT=9500
      - HTTP_PORT=9000
      - HTTP_BIND=0.0.0.0
      - PEERS=node-2=node-2:9500,node-3=node-3:9500,node-4=node-4:9500,node-5=node-5:9500
      - LICENSE_FAIL_MODE=open
      - IDEM_TOKEN_CAPACITY=5
    networks:
      raftkv-quickstart:
        aliases:
          - node-1
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/health/live"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 15s

  node-2:
    build: ..
    container_name: raft-node-2
    ports:
      - "9002:9000"
      - "9502:9500"
    environment:
      - SM4_KEY=raftkv_sm4test01
      - NODE_ID=node-2
      - GRPC_PORT=9500
      - HTTP_PORT=9000
      - HTTP_BIND=0.0.0.0
      - PEERS=node-1=node-1:9500,node-3=node-3:9500,node-4=node-4:9500,node-5=node-5:9500
      - LICENSE_FAIL_MODE=open
      - IDEM_TOKEN_CAPACITY=5
    networks:
      raftkv-quickstart:
        aliases:
          - node-2
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/health/live"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 15s

  node-3:
    build: ..
    container_name: raft-node-3
    ports:
      - "9003:9000"
      - "9503:9500"
    environment:
      - SM4_KEY=raftkv_sm4test01
      - NODE_ID=node-3
      - GRPC_PORT=9500
      - HTTP_PORT=9000
      - HTTP_BIND=0.0.0.0
      - PEERS=node-1=node-1:9500,node-2=node-2:9500,node-4=node-4:9500,node-5=node-5:9500
      - LICENSE_FAIL_MODE=open
      - IDEM_TOKEN_CAPACITY=5
    networks:
      raftkv-quickstart:
        aliases:
          - node-3
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/health/live"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 15s

  node-4:
    build: ..
    container_name: raft-node-4
    ports:
      - "9004:9000"
      - "9504:9500"
    environment:
      - SM4_KEY=raftkv_sm4test01
      - NODE_ID=node-4
      - GRPC_PORT=9500
      - HTTP_PORT=9000
      - HTTP_BIND=0.0.0.0
      - PEERS=node-1=node-1:9500,node-2=node-2:9500,node-3=node-3:9500,node-5=node-5:9500
      - LICENSE_FAIL_MODE=open
      - IDEM_TOKEN_CAPACITY=5
    networks:
      raftkv-quickstart:
        aliases:
          - node-4
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/health/live"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 15s

  node-5:
    build: ..
    container_name: raft-node-5
    ports:
      - "9005:9000"
      - "9505:9500"
    environment:
      - SM4_KEY=raftkv_sm4test01
      - NODE_ID=node-5
      - GRPC_PORT=9500
      - HTTP_PORT=9000
      - HTTP_BIND=0.0.0.0
      - PEERS=node-1=node-1:9500,node-2=node-2:9500,node-3=node-3:9500,node-4=node-4:9500
      - LICENSE_FAIL_MODE=open
      - IDEM_TOKEN_CAPACITY=5
    networks:
      raftkv-quickstart:
        aliases:
          - node-5
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/health/live"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 15s

networks:
  raftkv-quickstart:
    driver: bridge