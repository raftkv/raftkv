岱境235 确定性管控中枢 — ARM边缘盒子部署说明
====================================================

1. 第一步（提取指纹）：在黑框输入 ./gateway-arm64 并回车，把打印出的"机器指纹"复制发给我。
2. 第二步（放置钥匙）：收到我发回的 license.key 后，把它和 docker-compose.yml 放在同一目录下。
3. 第三步（正式启动）：在黑框输入 docker-compose up -d 并回车。